# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript 增量构建缓存
*.tsbuildinfo

# 防止 tsc 把 vite.config.ts 编译产物落盘到源目录，
# 进而遮蔽 Vite 实际加载的 vite.config.ts（曾导致 proxy 日志钩子不生效）
vite.config.js
vite.config.d.ts

# 环境变量（按需保留 .env.development / .env.production）
# 如需忽略所有 .env，取消下一行注释
# .env
